
import React from 'react';

const TechIconsContainer = (props) => {

  switch (props.icon) {
    case value:
      
      break;
  
    default:
      break;
  }

  return (
    <div>

    </div>
  )
}

export default TechIconsContainer;